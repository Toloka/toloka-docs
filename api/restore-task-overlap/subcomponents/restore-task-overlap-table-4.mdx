**string \| required**<br/><br/>The reason the Toloker lost access to the pool:

- `SKILL_CHANGE` — The skill level changed.
- `RESTRICTION` — The Toloker was blocked by a quality control rule (for example, for fast responses).