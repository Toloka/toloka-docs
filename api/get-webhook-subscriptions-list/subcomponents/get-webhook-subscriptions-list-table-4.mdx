**boolean**<br/><br/>Shows whether the list is complete.<br/><br/>Acceptable values:

- `true` — Not all elements are included in the output due to restrictions in the `limit` parameter.
- `false` — The output lists all the items.