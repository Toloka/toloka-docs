**array of objects**<br/><br/>Keys with the translation of the task interface elements.

```json
"key": "<key name>",
"value": "<target text>",
"source": "<source text>"
```

Translation source:

- `REQUESTER` — The requester set the value themselves.
