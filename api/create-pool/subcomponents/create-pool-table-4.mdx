**string \| required if**<br/><br/>Required if control tasks are assigned at a variable rate.<br/><br/>How to count tasks completed by the Toloker:

- `POOL` — Count completed pool tasks.
- `PROJECT` — Count completed project tasks.