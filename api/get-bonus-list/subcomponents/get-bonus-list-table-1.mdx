**string**<br/><br/>Parameters to sort by:

- `id` — Reward ID.
- `created` — The date when the reward was awarded, in UTC using ISO 8601 format: `YYYY-MM-DDThh:mm:ss[.sss]`.<br/><br/>To learn how to configure sorting, see [Sorting the list of objects](/docs/api/sorting).
