<If condition={props.platform == "web"}>

# Task list

</If><If condition={props.platform == "android"||props.platform==="ios"}>

# Selecting a task

</If><If condition={props.platform == "android"||props.platform==="ios"}>

Select:

* [Non-field task](#non-walk)
* [Field task](#walk)
* [Tips and recommendations](#android-list)

</If><If condition={props.platform == "android"||props.platform==="ios"}>

## Regular (non-field) tasks {#common}

To complete a non-field task:

1. Open the list of tasks by tapping <If condition={props.platform == "android"}>**<img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448834501/1676448834501_original_png.png" alt="" /> → Available**</If><If condition={props.platform == "ios"}>the button **All tasks**</If>.

1. Read the instructions and go to the task.

1. Tap <If condition={props.platform == "ios"}>**Start**</If><If condition={props.platform == "android"}>**Start**</If>.

1. Answer the questions and tap <If condition={props.platform == "ios"}>**Submit**</If><If condition={props.platform == "android"}>**Done**</If>.

You will be redirected to a page with the next task. <If condition={props.platform == "ios"}>To exit the task, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733323602/1676733323602_original_svg.svg" alt="" /> at the bottom of the screen and choose **Finish**</If><If condition={props.platform == "android"}>To exit the task, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733319521/1676733319521_original_svg.svg" alt="" /> at the top of the screen and choose **Finish**</If>.

</If><If condition={props.platform == "android"}>

## How to save uncompleted tasks {#howtosave}

You can save tasks that you didn't complete in **Reserved**. To do this:

1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448834501/1676448834501_original_png.png" alt="" /> → **Settings**.

1. Select the **Always reserve tasks to complete later** option

To continue performing the task, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448834501/1676448834501_original_png.png" alt="" /> → **Reserved**, find the task you need, and tap **Resume**.

</If>

<If condition={props.platform == "android"||props.platform==="ios"}>

## Field tasks {#fieldwork}

To complete a field task:

1. Open the task list. <If condition={props.platform == "android"}>To do this, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448834501/1676448834501_original_png.png" alt="" /> → **Available** </If><If condition={props.platform == "ios"}>To do this, tap **Tasks**</If>.

1. Use one of these ways to open task locations:

    * Select a task in the task list and tap <If condition={props.platform == "android"}>**Open map**</If><If condition={props.platform == "ios"}>**Open map**</If> to see the locations for this task.

    * Open the common map by tapping <If condition={props.platform == "android"}><img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448795481/1676448795481_original_png.png" alt="" /> at the bottom of the screen</If><If condition={props.platform == "ios"}> **Map**</If> to view locations with available tasks.

    <If condition={props.platform == "ios"}>

    <Note mode="tip" title="Tip">

    To save mobile data, download the map of the region to your device. To do this, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448782111/1676448782111_original_png.png" alt="" />.

    To delete the downloaded maps, open **More** → **Settings** → **Delete downloaded maps**.

    </Note>

    </If>

    To return to the task list, tap <If condition={props.platform == "android"}> <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448711664/1676448711664_original_png.png" alt="" /> at the bottom of the screen </If><If condition={props.platform == "ios"}> the **List** button at the top of the screen</If>.

1. Tap a placemark with a task.

    <If condition={props.platform == "android"}>If there are several tasks in one spot, use the search function. To find a task in the list:

    1. Pull the list up.

    1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733320430/1676733320430_original_svg.svg" alt="" /> on the top right of the screen.

    1. Start entering the task name or description.

    </If>

    To set a minimum price per task:

    1. <If condition={props.platform == "android"}>Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448913193/1676448913193_original_png.png" alt="" /> at the bottom of the screen</If><If condition={props.platform == "ios"}>Tap **Filters** at the top of the screen</If>.

    1. Move the slider.

    1. Tap <If condition={props.platform == "android"}>**Apply**</If><If condition={props.platform == "ios"}>**Done**</If>.

1. Read the instructions.

1. Reserve a task by tapping <If condition={props.platform == "ios"}>**Reserve**</If><If condition={props.platform == "android"}>**Reserve**</If>. The task is displayed on the <If condition={props.platform == "android"}>**Reserved**</If><If condition={props.platform == "ios"}>**My tasks**</If> page. The countdown starts from this moment. The timer is displayed in the task header next to the <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733320837/1676733320837_original_svg.svg" alt="" /> icon. Max duration is specified in the task card.

    You can reserve several tasks at once.

1. <If condition={props.platform == "android"}>To build a route, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448902899/1676448902899_original_png.png" alt="" /> → **Get directions**.</If><If condition={props.platform == "ios"}>Create a route to the location by tapping **Get directions**.</If> The route is created in the [Yandex Maps](https://itunes.apple.com/us/app/yandex-maps/id313877526) app.

1. Go to the location shown in the task. Next, select the desired task on the <If condition={props.platform == "ios"}>**My tasks → Active** page and tap **Continue**. </If><If condition={props.platform == "android"}>**Reserved** and tap **Continue**</If>

1. Perform the task:

    * Re-read the instructions.

    * Answer the questions and attach a photo (if required).

    * Tap <If condition={props.platform == "ios"}>**Submit**</If><If condition={props.platform == "android"}>**Done**.</If>

        <Note mode="warning" title="Warning">

        You need to tap <If condition={props.platform == "ios"}>**Submit**</If><If condition={props.platform == "android"}> **Done**</If> while you are at the location shown in the task. Otherwise, your response might be rejected.

        </Note>

The task with the responses is saved on the <If condition={props.platform == "android"}>**Done**</If><If condition={props.platform == "ios"}>**My tasks → Done**</If> page until you connect to Wi-Fi, if you set the <If condition={props.platform == "android"}>**Submit over Wi-Fi only}**</If><If condition={props.platform == "ios"}>**Submit over Wi-Fi only**</If> option in the [settings](/docs-new/tolokers/web/settings). You need to make sure the task is submitted before the task time expires.

</If>

<If condition={props.platform == "android"||props.platform==="ios"}>

### How do I make changes to a task? {#makechanges}

If a task is completed but isn't sent yet, you can make changes to it:

1. Open the task on the <If condition={props.platform == "android"}>**Done**</If><If condition={props.platform == "ios"}>**My tasks → Done**</If> page.

1. <If condition={props.platform == "android"}>Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733319521/1676733319521_original_svg.svg" alt="" /> at the top of the screen and select **Change status to reserved**.</If><If condition={props.platform == "ios"}>Tap **Change to Active**.</If>

1. Go to the <If condition={props.platform == "android"}>**Reserved**</If><If condition={props.platform == "ios"}>**My tasks → Active**</If> page and <If condition={props.platform == "android"}>make changes to the task</If><If condition={props.platform == "ios"}>tap **Continue**</If>.

1. <If condition={props.platform == "android"}>Tap **Done**</If>

   <If condition={props.platform == "ios"}>Make changes to the task and tap **Submit**</If>.

</If><If condition={props.platform == "android"||props.platform==="ios"}>

### How do I submit a task? {#sendtask}

Task responses are sent to the requester as soon as you have internet access. To send, you need to open the mobile app. If the <If condition={props.platform == "android"}>**Submit over Wi-Fi only**</If><If condition={props.platform == "ios"}>**Submit over Wi-Fi only**</If> option is chosen in the [settings](/docs-new/tolokers/settings.dita), the task is sent after you connect to Wi-Fi. <If condition={props.platform == "android"||props.platform==="ios"}>To submit a task over mobile internet:</If>

<If condition={props.platform == "android"||props.platform==="ios"}>

1. Open the task on the <If condition={props.platform == "android"}> **Done**</If><If condition={props.platform == "ios"}>**My tasks → Done**</If> page.

1. Tap <If condition={props.platform == "android"}><img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733319521/1676733319521_original_svg.svg" alt="" /> at the top of the screen and choose **Submit**</If><If condition={props.platform == "ios"}>**Send now**</If>.

</If>

<If condition={props.platform == "android"||props.platform==="ios"}>

## Tips and recommendations {#tips}

The list of tasks displays all tasks, including both field and non-field tasks. To open the list of tasks, tap
<If condition={props.platform == "android"}><img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448834501/1676448834501_original_png.png" alt="" /> → **Available**</If><If condition={props.platform == "ios"}> **Tasks**</If>.

<If condition={props.platform == "android"}>To manage the list of tasks, apply filters and configure sorting:

1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448913193/1676448913193_original_png.png" alt="" /> at the top of the screen.

1. Choose one or more filters. Choose the requesters you want to see in the list. Set the sorting type.

1. Tap **Apply**.

</If>

<If condition={props.platform == "ios"}>To manage your task list:

1. Tap **Filters** at the top of the screen.

1. Use filters or sorting tools:

    Task price

    : To set the minimum task price, move the slider.

    Task categories

    : Enable or disable categories such as **Field task** or **with training**.

    Requesters

    : Tap a requester to add their tasks to the list or exclude them from the list.

    Sorting

    : Choose the sorting type: by price, last updated, or Favorites.

1. Tap **Done** at the top of the page.

</If>

Unavailable tasks are marked with the <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733315411/1676733315411_original_svg.svg" alt="" /> icon. The condition for accessing the task is indicated next to it. Some tasks can only be performed in the browser on the [toloka.yandex.com](http://toloka.yandex.com/) website.

<If condition={props.platform == "android"||props.platform==="ios"}>

### How to add a task to favorites {#favourites}

<If condition={props.platform == "android"||props.platform==="ios"}>Mark the tasks that you liked:

1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733323602/1676733323602_original_svg.svg" alt="" /> in the task card.

2. Select <If condition={props.platform == "web"}>**Add to favorites**</If><If condition={props.platform == "android"}>**Add to favorites**</If><If condition={props.platform == "ios"}>**Add to favorites**</If>.

</If>

<If condition={props.platform == "android"}> To place your favorite tasks at the top of the list, set the **Favorites first** [sorting](#android-list/list-settings-android) type.</If>

<If condition={props.platform == "ios"}> To place your favorite tasks at the top of the list, set the **Favorites first** [sorting](#android-list/list-settings-ios) type.</If>

</If>

<If condition={props.platform == "android"||props.platform==="ios"}>

### How do I hide a task? {#hide}

<If condition={props.platform == "android"||props.platform==="ios"}> Hide a task so that it doesn't appear in the list.

1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733323602/1676733323602_original_svg.svg" alt="" /> in the task card.

1. Select **Hide task**.

</If>

</If>

<If condition={props.platform == "android"||props.platform==="ios"}>

### How to change pin appearance {#changelook}

To choose which task information to display in the label:

1. <If condition={props.platform == "ios"}>Tap **More → Settings** </If>

    <If condition={props.platform == "android"}>Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676448834501/1676448834501_original_png.png" alt="" /> → **Settings**</If>

1. Open <If condition={props.platform == "android"}>**Change pin appearance**</If><If condition={props.platform == "ios"}>**Change pin appearance**</If> and choose the desired option.

<If condition={props.platform == "android"}>

1. Tap **Save**.

</If>

</If>

</If>

<If condition={props.platform == "web"}>

Open the [Tasks](https://toloka.yandex.ru/tasks) page. By default, tasks are displayed according to the **favorites first** criteria. You can set a different sorting order in the row above:

- **newest first**
- **price**
- **favorites first**

You can also filter tasks by category (with training, with post acceptance, hidden, and so on) and by requester.

Unavailable tasks are marked with the <img src="https://yastatic.net/s3/doc-binary/freeze/ru/toloka-common/ffff7f0618be7fe3d467ae1da45110b236860ecd.svg" alt="" /> icon. The condition for accessing the task is indicated next to it. For example, the "mobile app" condition means that the task can't be completed in the desktop version of Toloka, only in the mobile app.
</If>

<If condition={props.platform == "web"}>

## Favorites and hidden tasks {#selected-n-hidden}

You can add a task to your favorites or hide it in the list.

**Add task to favorites**

- Hover over the task card and click <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733319521/1676733319521_original_svg.svg" alt="" /> on the right.

- Select <If condition={props.platform == "web"}>**Add to favorites**</If><If condition={props.platform == "android"}>**Add to favorites**</If><If condition={props.platform == "ios"}>**Add to favorites**</If>.

    Or click <img width="15px" src="1676448866956" alt="" /> next to the task name.

Click **favorites first** to display the selected tasks at the top of the list. If the tasks in the favorites list are inactive, you might have run out of them, or made a lot of mistakes, and the customer restricted your access to them. When the new tasks appear, they will be active again.

<Note mode="tip" title="Tip">

There are a lot of tasks in Toloka, and new tasks appear every day. Therefore, check the list of tasks regularly to find something you like.

</Note>

**Hide a task**

- Hover over the task card and click <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733319521/1676733319521_original_svg.svg" alt="" /> on the right.

- Select **Hide this task**.

- Turn off the **Hidden** filter.

**Remove a task from the hidden**

- Turn on the **Hidden** filter.

- Hover over the task card and click <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676733319521/1676733319521_original_svg.svg" alt="" /> on the right.

- Select **Show this task again**.

</If>

</If>
