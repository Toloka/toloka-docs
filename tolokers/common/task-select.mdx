{% if platform == "web" %}
# Task list
{% elsif platform == "android" or platform == "ios" %}
# Selecting a task
{% endif %}

{% if platform == "android" or platform == "ios" %}
Select:

* [Non-field task](#non-walk)
* [Field task](#walk)
* [Tips and recommendations](#android-list)
   {% endif %}

{% if platform == "android" or platform == "ios" %}
## Regular (non-field) tasks {#common}

To complete a non-field task:

1. Open the list of tasks by tapping {% if platform == "android" %}**<Image withBackground imageId="1676448834501" alt="" /> → Available**{% elsif platform == "ios" %}the button **All tasks**{% endif %}.
1. Read the instructions and go to the task.
1. Tap {% if platform == "ios" %}**Start**{% elsif platform == "android" %}**Start**{% endif %}.
1. Answer the questions and tap {% if platform == "ios" %}**{{ mobile_ios_submit_button}}**{% elsif platform == "android" %}**Done**{% endif %}.

You will be redirected to a page with the next task.
{% if platform=='ios' %}To exit the task, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449129896/1676449129896_original_svg.svg" alt="" /> at the bottom of the screen and choose **Finish**{% elsif platform== "android" %}To exit the task, tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449125825/1676449125825_original_svg.svg" alt="" /> at the top of the screen and choose **Finish**{% endif %}.

{% endif %}

{% if platform == "android" %}
## How to save uncompleted tasks {#howtosave}

You can save tasks that you didn't complete in **Reserved**. To do this:

1. Tap <Image withBackground imageId="1676448834501" alt="" /> → **Settings**.
1. Select the **Always reserve tasks to complete later** option

To continue performing the task, tap <Image withBackground imageId="1676448834501" alt="" /> → **Reserved**, find the task you need, and tap **Resume**.
{% endif %}

{% if platform == "android" or platform == "ios" %}
## Field tasks {#fieldwork}

To complete a field task:

1. Open the task list. {% if platform == "android" %}To do this, tap <Image withBackground imageId="1676448834501" alt="" /> → **Available** {% elsif platform == "ios" %}To do this, tap **Tasks**{% endif %}.
1. Use one of these ways to open task locations:

   * Select a task in the task list and tap {% if platform == "android" %}**Open map**{% elsif platform == "ios"%}**Open map**{% endif %} to see the locations for this task.
   * Open the common map by tapping {% if platform == "android" %}<Image withBackground imageId="1676448795481" alt="" /> at the bottom of the screen{% elsif platform == "ios"%} **Map**{% endif %} to view locations with available tasks.

   {% if platform == "ios" %}

   <Note mode="tip" title="Tip">

   To save mobile data, download the map of the region to your device. To do this, tap <Image withBackground imageId="1676448782111" alt="" />.

   To delete the downloaded maps, open **More** → **Settings** → **Delete downloaded maps**.

   </Note>

   {% endif %}

   To return to the task list, tap {% if platform == "android" %} <Image withBackground imageId="1676448711664" alt="" /> at the bottom of the screen {% elsif platform == "ios" %} the **List** button at the top of the screen{% endif %}.

1. Tap a placemark with a task.

   {% if platform == "android" %}If there are several tasks in one spot, use the search function. To find a task in the list:

   1. Pull the list up.
   1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449126636/1676449126636_original_svg.svg" alt="" /> on the top right of the screen.
   1. Start entering the task name or description.
      {% endif %}

   To set a minimum price per task:

   1. {% if platform == "android" %}Tap <Image withBackground imageId="1676448913193" alt="" /> at the bottom of the screen{% elsif platform == "ios" %}Tap **Filters** at the top of the screen{% endif %}.
   1. Move the slider.
   1. Tap {% if platform == "android"%}**Apply**{% elsif platform == "ios"%}**Done**{% endif %}.

1. Read the instructions.

1. Reserve a task by tapping {% if platform == "ios" %}**Reserve**{% endif %}{% if platform == "android" %}**Reserve**{% endif %}. The task is displayed on the {% if platform == "android" %}**Reserved**{% endif %}{% if platform == "ios" %}**My tasks**{% endif %} page. The countdown starts from this moment. The timer is displayed in the task header next to the <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449127157/1676449127157_original_svg.svg" alt="" /> icon. Max duration is specified in the task card.

   You can reserve several tasks at once.

1. {% if platform == "android" %}To build a route, tap <Image withBackground imageId="1676448902899" alt="" /> → **Get directions**.{% endif %}{% if platform == "ios" %}Create a route to the location by tapping **Get directions**.{% endif %} The route is created in the [Yandex Maps]() app.

1. Go to the location shown in the task. Next, select the desired task on the {% if platform == "ios" %}**My tasks → Active** page and tap **Continue**. {% endif %}{% if platform == "android" %}**Reserved** and tap **Continue**{% endif %}

1. Perform the task:
   * Re-read the instructions.
   * Answer the questions and attach a photo (if required).
   * Tap {% if platform == "ios" %}**Submit**{% elsif platform == "android" %}**Done**.{% endif %}

      <Note mode="warning" title="Warning">

      You need to tap {% if platform == "ios" %}**Submit**{% elsif platform == "android" %} **Done**{% endif %} while you are at the location shown in the task. Otherwise, your response might be rejected.

      </Note>

The task with the responses is saved on the {% if platform == "android" %}**Done**{% elsif platform == "ios" %}**My tasks → Done**{% endif %} page until you connect to Wi-Fi, if you set the {% if platform == "android"%}**Submit over Wi-Fi only}**{% elsif platform == "ios" %}**Submit over Wi-Fi only**{% endif %} option in the [settings](/docs-new/tolokers/settings.dita). You need to make sure the task is submitted before the task time expires.
{% endif %}

{% if platform == "android" or platform == "ios" %}
### How do I make changes to a task? {#makechanges}

If a task is completed but isn't sent yet, you can make changes to it:

1. Open the task on the {% if platform == "android" %}**Done**{% elsif platform== "ios" %}**My tasks → Done**{% endif %} page.
1. {% if platform == "android" %}Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449125825/1676449125825_original_svg.svg" alt="" /> at the top of the screen and select **Change status to reserved**.{% elsif platform == "ios" %}Tap **Change to Active**.{% endif %}
1. Go to the {% if platform == "android" %}**Reserved**{% elsif platform == "ios" %}**My tasks → Active**{% endif %} page and {% if platform == "android" %}make changes to the task{% endif %}{% if platform == "ios" %}tap **Continue**{% endif %}.
1. {% if platform == "android" %}Tap **Done**{% endif %}
   {% if platform == "ios" %}Make changes to the task and tap **Submit**{% endif %}.

{% endif %}

{% if platform == "android" or platform == "ios" %}
### How do I submit a task? {#sendtask}

Task responses are sent to the requester as soon as you have internet access. To send, you need to open the mobile app. If the {% if platform == "android" %}**Submit over Wi-Fi only**{% elsif platform == "ios" %}**{{ mobile_ios_settings_wifi_title }**{% endif %} option is chosen in the [settings](/docs-new/tolokers/settings.dita), the task is sent after you connect to Wi-Fi. {% if platform == "android" or platform == "ios" %}To submit a task over mobile internet:{% endif %}

{% if platform == "android" or platform == "ios" %}
1. Open the task on the {% if platform == "android" %} **Done**{% elsif platform== "ios" %}**My tasks → Done**{% endif %} page.
1. Tap {% if platform == "android" %}<img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449125825/1676449125825_original_svg.svg" alt="" /> at the top of the screen and choose **Submit**{% elsif platform== "ios" %}**Send now**{% endif %}.

{% endif %}

{% if platform == "android" or platform == "ios" %}
## Tips and recommendations {#tips}

The list of tasks displays all tasks, including both field and non-field tasks. To open the list of tasks, tap
{% if platform == "android" %}<Image withBackground imageId="1676448834501" alt="" /> → **Available**{% endif %}
{% if platform == "ios" %} **Tasks**{% endif %}.

{% if platform == "android" %}To manage the list of tasks, apply filters and configure sorting:

1. Tap <Image withBackground imageId="1676448913193" alt="" /> at the top of the screen.
1. Choose one or more filters. Choose the requesters you want to see in the list. Set the sorting type.
1. Tap **Apply**.

{% endif %}
{% if platform == "ios" %}To manage your task list:
1. Tap **Filters** at the top of the screen.
1. Use filters or sorting tools:

   Task price

   : To set the minimum task price, move the slider.

   Task categories

   : Enable or disable categories such as **Field task** or **with training**.

   Requesters

   : Tap a requester to add their tasks to the list or exclude them from the list.

   Sorting

   : Choose the sorting type: by price, last updated, or Favorites.

1. Tap **Done** at the top of the page.

{% endif %}

Unavailable tasks are marked with the <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449121627/1676449121627_original_svg.svg" alt="" /> icon. The condition for accessing the task is indicated next to it. Some tasks can only be performed in the browser on the [toloka.yandex.com](http://toloka.yandex.com/){% else %}[toloka.yandex.ru](http://toloka.yandex.com/).
{% endif %} website.

{% if platform == "android" or platform == "ios" %}
### How to add a task to favorites {#favourites}

{% if platform == "android" or platform == "ios" %}Mark the tasks that you liked:

1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449129896/1676449129896_original_svg.svg" alt="" /> in the task card.
2. Select {% if platform == "web" %}**Add to favorites**{% elsif platform== "android" %}**Add to favorites**{% elsif platform == "ios" %}**Add to favorites**{% endif %}.
   {% endif %}

{% if platform == "android" %} To place your favorite tasks at the top of the list, set the **Favorites first** [sorting](#android-list/list-settings-android) type.{% endif %}
{% if platform == "ios" %} To place your favorite tasks at the top of the list, set the **Favorites first** [sorting](#android-list/list-settings-ios) type.{% endif %}
{% endif %}

{% if platform == "android" or platform == "ios" %}
### How do I hide a task? {#hide}
{% if platform == "android" or platform == "ios" %} Hide a task so that it doesn't appear in the list.
1. Tap <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449129896/1676449129896_original_svg.svg" alt="" /> in the task card.
1. Select **Hide task**.
   {% endif %}
   {% endif %}

{% if platform == "android" or platform == "ios" %}
### How to change pin appearance {#changelook}

To choose which task information to display in the label:

1. {% if platform == "ios" %}Tap **More → Settings** {% endif %}
   {% if platform == "android" %}Tap <Image withBackground imageId="1676448834501" alt="" /> → **Settings**{% endif %}
1. Open {% if platform == "android" %}**{{ mobile_android_settings_pin_view_type_title}}**{% elsif platform== "ios" %}**Change pin appearance**{% endif %} and choose the desired option.
   {% if platform == "android" %}
1. Tap **Save**.
   {% endif %}
   {% endif %}
   {% endif %}

{% if platform == "web" %}
Open the [Tasks](https://toloka.yandex.ru/tasks) page. By default, tasks are displayed according to the **favorites first** criteria. You can set a different sorting order in the row above:

- **newest first**
- **price**
- **favorites first**

You can also filter tasks by category (with training, with post acceptance, hidden, and so on) and by requester.

Unavailable tasks are marked with the <img src="https://yastatic.net/s3/doc-binary/freeze/ru/toloka-common/ffff7f0618be7fe3d467ae1da45110b236860ecd.svg" alt="" /> icon. The condition for accessing the task is indicated next to it. For example, the "mobile app" condition means that the task can't be completed in the desktop version of Toloka, only in the mobile app.
{% endif %}

{% if platform== "web" %}

## Favorites and hidden tasks {#selected-n-hidden}

You can add a task to your favorites or hide it in the list.

**Add task to favorites**

- Hover over the task card and click <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449125825/1676449125825_original_svg.svg" alt="" /> on the right.
- Select {% if platform == "web" %}**Add to favorites**{% elsif platform == "android" %}**Add to favorites**{% elsif platform == "ios" %}**Add to favorites**{% endif %}.
   Or click <Image width="15px" withBackground imageId="1676448866956" alt="" /> next to the task name.

Click **favorites first** to display the selected tasks at the top of the list. If the tasks in the favorites list are inactive, you might have run out of them, or made a lot of mistakes, and the customer restricted your access to them. When the new tasks appear, they will be active again.

<Note mode="tip" title="Tip">

There are a lot of tasks in Toloka, and new tasks appear every day. Therefore, check the list of tasks regularly to find something you like.

</Note>

**Hide a task**

- Hover over the task card and click <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449125825/1676449125825_original_svg.svg" alt="" /> on the right.
- Select **Hide this task**.
- Turn off the **Hidden** filter.

**Remove a task from the hidden**

- Turn on the **Hidden** filter.
- Hover over the task card and click <img src="https://tlk-infra-front.azureedge.net/portal-static/uploaded/images/1676449125825/1676449125825_original_svg.svg" alt="" /> on the right.
- Select **Show this task again**.

{% endif %}