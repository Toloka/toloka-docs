The order and direction of sorting the results.

Available parameters:

* `id` - by id;
* `name` - by name;
* `created` — by the creation date. The date and time is specified in UTC in ISO 8601 format: `YYYY-MM-DDThh:mm:ss[.sss]`.

You can specify multiple parameters separated by a comma.

To change the sorting direction to descending, add a hyphen before the parameter. For example, sort=-id.
