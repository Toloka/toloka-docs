---
title: "Add items in bulk — Apps API | Toloka documentation"
date: "2023-02-06T18:34:34.910574"
author: ["Toloka Team"]
coverId: F_bIcOYF9yjrxkPZrXc3
tags: ["engineering"]
ogDescription: ""
docsMenu: "appsApiMenu"
---

import AppProjectsappProjectIditemsbulkPostTable1 from './app-projectsapp-project-iditemsbulk-post/subcomponents/app-projectsapp-project-iditemsbulk-post-table-1.mdx'

# Add items in bulk

## Description

Adds multiple items to a batch in the project with the ID specified in the request, with the maximum of 10,000 items per request.

<Note mode="tip" title="Tip">

**Try our [new API reference](https://toloka.ai/docs/api/apps-reference/#post-/app-projects/-app_project_id-/items/bulk):** more parameter details, request/response examples, and code samples in various programming languages.

</Note>

## Request

method | url
----- | -----
post | <AppProjectsappProjectIditemsbulkPostTable1/>

### Parameters

name | type | required | description
----- | ----- | ----- | -----
app_project_id | path | true | Project ID.

## Responses

### 200

#### Description

OK

<Cut title="application/json">

```json
{
    "schema": {
        "type": "array",
        "items": {
            "type": "string",
            "description": "ID of the uploaded task item."
        }
    }
}
```

</Cut>

### 400

#### Description

Error in the request data.

<Cut title="application/json">

```json
{
    "schema": {
        "type": "object",
        "required": [
            "request_id",
            "code",
            "message"
        ],
        "properties": {
            "request_id": {
                "type": "string",
                "example": "337d68d1-974d-42b1-a2d0-6234f6373eed",
                "description": "Internal ID of the HTTP request where the error occurred. It needs to be specified when contacting support."
            },
            "code": {
                "type": "string",
                "description": "String error code.",
                "example": "DOES_NOT_EXIST"
            },
            "message": {
                "type": "string",
                "description": "Detailed description of the error.",
                "example": "The object doesn't exist"
            },
            "payload": {
                "type": "object",
                "description": "Additional information about the error. May have a different structure for different errors.",
                "nullable": true,
                "example": {
                    "instruction_class": "dog",
                    "instruction_field": "example"
                }
            }
        }
    }
}
```

</Cut>

### 403

#### Description

No authorization to perform this operation.

<Cut title="application/json">

```json
{
    "schema": {
        "type": "object",
        "required": [
            "request_id",
            "code",
            "message"
        ],
        "properties": {
            "request_id": {
                "type": "string",
                "example": "337d68d1-974d-42b1-a2d0-6234f6373eed",
                "description": "Internal ID of the HTTP request where the error occurred. It needs to be specified when contacting support."
            },
            "code": {
                "type": "string",
                "description": "String error code.",
                "example": "DOES_NOT_EXIST"
            },
            "message": {
                "type": "string",
                "description": "Detailed description of the error.",
                "example": "The object doesn't exist"
            },
            "payload": {
                "type": "object",
                "description": "Additional information about the error. May have a different structure for different errors.",
                "nullable": true,
                "example": {
                    "instruction_class": "dog",
                    "instruction_field": "example"
                }
            }
        }
    }
}
```

</Cut>

### 404

#### Description

Task items not found.

<Cut title="application/json">

```json
{
    "schema": {
        "type": "object",
        "required": [
            "request_id",
            "code",
            "message"
        ],
        "properties": {
            "request_id": {
                "type": "string",
                "example": "337d68d1-974d-42b1-a2d0-6234f6373eed",
                "description": "Internal ID of the HTTP request where the error occurred. It needs to be specified when contacting support."
            },
            "code": {
                "type": "string",
                "description": "String error code.",
                "example": "DOES_NOT_EXIST"
            },
            "message": {
                "type": "string",
                "description": "Detailed description of the error.",
                "example": "The object doesn't exist"
            },
            "payload": {
                "type": "object",
                "description": "Additional information about the error. May have a different structure for different errors.",
                "nullable": true,
                "example": {
                    "instruction_class": "dog",
                    "instruction_field": "example"
                }
            }
        }
    }
}
```

</Cut>

### 413

#### Description

The request is too large. The maximum number of items per request is 10,000.

<Cut title="application/json">

```json
{
    "schema": {
        "type": "object",
        "required": [
            "request_id",
            "code",
            "message"
        ],
        "properties": {
            "request_id": {
                "type": "string",
                "example": "337d68d1-974d-42b1-a2d0-6234f6373eed",
                "description": "Internal ID of the HTTP request where the error occurred. It needs to be specified when contacting support."
            },
            "code": {
                "type": "string",
                "description": "String error code.",
                "example": "DOES_NOT_EXIST"
            },
            "message": {
                "type": "string",
                "description": "Detailed description of the error.",
                "example": "The object doesn't exist"
            },
            "payload": {
                "type": "object",
                "description": "Additional information about the error. May have a different structure for different errors.",
                "nullable": true,
                "example": {
                    "instruction_class": "dog",
                    "instruction_field": "example"
                }
            }
        }
    }
}
```

</Cut>
