import TolokaRequesterPoolType from './pool-type.mdx'
import TolokaRequesterPoolName from './pool-name.mdx'
import TolokaRequesterPoolDescription from './pool-description.mdx'
import TolokaRequesterPoolWizardApply from './pool-wizard-apply.mdx'
import TolokaRequesterSourceQualityControl from './quality-control.mdx'
import TolokaRequesterSourceTolokersInterest from './tolokers-interest.mdx'
import TolokaRequesterSourceStepEnabled from './step-enabled.mdx'
import TolokaRequesterSourcePoolIsReady from './pool-is-ready.mdx'

1. Click **Add a pool** on the project page.

1. <TolokaRequesterPoolType/>

1. <TolokaRequesterPoolName/>

1. <TolokaRequesterPoolDescription/>

1. Click **Create**.

1. Follow the steps in the pool wizard:

    <TolokaRequesterPoolWizardApply/>

    1. **Select the audience for your task**

        Select Tolokers using [filters](/docs/glossary#filters). Tasks will only be shown to matching Tolokers, rather than to all of them.

        Click the **Add filter** button and set the filter parameters.

    1. **Setup quality control**

        <TolokaRequesterSourceQualityControl/>

        To learn more, see the [Quality control overview](/docs/guide/quality-control-overview) section.

    1. **Set the task price and overlap**

        In the **Price per task suite, $** field, set the amount of money to pay per task suite done by one Toloker.

        <TolokaRequesterSourceTolokersInterest/>

        In the **Overlap** field, define how many Tolokers must do each task.

        You can also set up [dynamic pricing](/docs/glossary#dynamic-pricing) and [dynamic overlap](/docs/glossary#dynamic-overlap).

    1. **Prepare and upload data**

        [Upload tasks](/docs/guide/task_upload) to the pool. This step will be enabled after you complete the previous steps.

    1. **Add control tasks for checking performance**

        Add [control tasks](/docs/glossary#control-task) to monitor the quality of task completion:

        1. Enter correct responses.

        2. Select the fields to use.

        3. Click **Save and go to next** or **Skip task**.

        4. Click **Save all control tasks**.

        <TolokaRequesterSourceStepEnabled/>

    1. **Double-check your project and try out tasks**

        At this step, check how the task will look from the Toloker's point of view.

        <TolokaRequesterSourceStepEnabled/>

    1. **Add optional pool settings**

        Set up advanced pool settings.

        <Note mode="note" title="Note">

        This step will be enabled after you complete the previous steps. You can skip this step by clicking **Use default settings**.

        </Note>

<TolokaRequesterSourcePoolIsReady/>
