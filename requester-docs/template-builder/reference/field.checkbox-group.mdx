---
title: "field.checkbox-group — Template Builder | Toloka documentation"
date: "2023-05-26T15:52:00.088949"
author: ["Toloka Team"]
coverId: F_bIcOYF9yjrxkPZrXc3
tags: ["engineering"]
ogDescription: ""
docsMenu: "defaultMenu"
---

import TolokaTbSourceDataWithValues from '../subcomponents/toloka-tb-source/id-toloka-tb-source/data-with-values.mdx'

# field.checkbox-group

Adds a group of checkboxes for selecting independent answer options. This is useful if there are several options. You can add just one checkbox, but it's easier to use [field.checkbox](/docs/template-builder/reference/field.checkbox) for that.

<Button themeColor="light" view="default" size="l" type="link" url="https://ya.cc/t/qeQoTGCe47BNW2">View example</Button>

<Cut title="Components used in the example">

- [data.output](/docs/template-builder/operations/work-with-data): The output data. This is what you get when a Toloker clicks the **Submit** button.
- [condition.required](/docs/template-builder/reference/condition.required): Checks if at least one option is selected.

</Cut>

By default, if the component returns `false`, this result will not be added to the output. To add `false` to the results, specify `"preserveFalse": true`.

<Button themeColor="light" view="default" size="l" type="link" url="https://ya.cc/t/Mo_S234j47BPYs">View example</Button>

<Cut title="Components used in the example">

- [data.output](/docs/template-builder/operations/work-with-data): The output data. This is what you get when a Toloker clicks the **Submit** button.
- [condition.any](/docs/template-builder/reference/condition.any): Checks that at least one of the child conditions is met.
- [condition.equals](/docs/template-builder/reference/condition.equals): Checks whether the original value is equal to the specified value.

</Cut>

## Component properties {#properties}

**Name** | **Type** | **Description**
----- | ----- | -----
`type`<span style="color: red">\*</span> | "field.checkbox-group" | Set component type.
`data`<span style="color: red">\*</span> | _writable_ | <TolokaTbSourceDataWithValues/>
`label` | _string_ | Label above the component.
`disabled` | _boolean_ | If `true`, the options are inactive.
`hint` | _string_ | Hint text.
`options`<span style="color: red">\*</span> | _array_ | An array of checkboxes, where `value` is the value to change, and `label` is the text near the checkbox.
`options[]` | _object_ | A checkbox.
`options[].hint` | _string_ | Text with additional information about the option.
`options[].label`<span style="color: red">\*</span> | _string_ | Text next to the option.
`options[].value`<span style="color: red">\*</span> | _string_ | Returned value.
`preserveFalse` | _boolean_ | Property that specifies whether to return `false` values in the results. By default, if the component returns `false`, this result will not be added to the output. To add `false` to the results, specify `"preserveFalse": true`.
`validation` | _condition_ | Validation based on condition.

<Button themeColor="light" view="default" size="l" type="link" url="/docs/guide/troubleshooting/support?form-source=template-builder-reference-field-checkbox-group">Contact support</Button>