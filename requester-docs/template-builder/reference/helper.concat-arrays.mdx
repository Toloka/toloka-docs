---
title: "helper.concat-arrays — Template Builder | Toloka documentation"
date: "2023-08-25T12:03:11.428312"
coverId: F_bIcOYF9yjrxkPZrXc3
docsMenu: "defaultMenu"
---

# helper.concat-arrays

Merging multiple arrays into a single array.

For example, let's say you have multiple arrays:

```json
([1, 2, 3], [4, 5, 6], [7, 8, 9])
```

Their elements can be combined into a single array to show simultaneously:

```json
[1, 2, 3, 4, 5, 6, 7, 8, 9]
```

<Button view="defaultInverse" size="l" type="link" url="https://ya.cc/t/5gzayPK_3tz4vn">View example</Button>

<Cut title="Components used in the example">

- [view.list](/docs/template-builder/reference/view.list): Displays data in a list.
- [view.text](/docs/template-builder/reference/view.text): Displays a text.

</Cut>

## Component properties `{#properties}`

**Name** | **Type** | **Description**
----- | ----- | -----
`type`<span style={{color: "red"}}>\*</span> | "helper.concat-arrays" | Set component type.
`items` | _array_ | Arrays to combine.
`items[]` | _any_ | Array element.

<Button view="defaultInverse" size="l" type="link" url="/docs/guide/troubleshooting/support?form-source=template-builder-reference-helper-concat-arrays">Contact support</Button>