---
title: "Playing back audio — Template Builder | Toloka documentation"
date: "2023-08-02T12:06:48.041351"
coverId: F_bIcOYF9yjrxkPZrXc3
docsMenu: "defaultMenu"
---

import TolokaTbSourceAddMedia from '../subcomponents/toloka-tb-source/id-toloka-tb-source/add-media.mdx'
import PlayAudioList1 from './play-audio/subcomponents/play-audio-list-1.mdx'
import PlayAudioList2 from './play-audio/subcomponents/play-audio-list-2.mdx'

# Playing back audio

In this section, we'll show how you can [embed an audio player](#insert-to-interface) into the interface, and how you can make sure that a Toloker [listened](#validate-listened) to your file.

<TolokaTbSourceAddMedia/>

## Add an audio player to the interface `{#insert-to-interface}`

To insert an audio file into the task interface, add the [view.audio](/docs/template-builder/reference/view.audio) component to the template. In the `url` property, specify a direct link to the file (we recommend that you use the MP3 format):

```json
{
  "type": "view.audio",
  "url": "http://example.com/audio.mp3"
}
```

If you pass a link to an audio file in the [input data](/docs/template-builder/operations/work-with-data), use the `data.input` component in the `url` property.

To loop audio playback automatically, put `true` in the `loop` property.

<Button themeColor="light" view="default" size="l" type="link" url="https://ya.cc/t/hn12lyBE3ttC7a">View example</Button>

## Make sure that the Toloker listened to the audio `{#validate-listened}`

<Tabs tabs={[
  { title: 'Started listening', value: <PlayAudioList1/> },
  { title: 'Listened completely', value: <PlayAudioList2/> },
]}/>

## Record audio `{#record-audio}`

To allow Tolokers to upload their audio files, use the [field.audio](/docs/template-builder/reference/field.audio) component.

On the website, `field.audio` allows Tolokers to upload audio files, and in the app, it opens the sound recorder.

```json
{
  "type": "field.audio",
  "data": {
    "type": "data.output",
    "path": "path",
  }
}
```

<Button themeColor="light" view="default" size="l" type="link" url="https://ya.cc/t/uDWbPPCP3ttFAM">View example</Button>

## Create a task to transcribe audio `{#transform-to-text}`

To create a template for audio transcription tasks, we used the following components:

- [condition.played](/docs/template-builder/reference/condition.played): To make sure that a Toloker listened to the audio.
- [condition.required](/docs/template-builder/reference/condition.required): To make sure that a Toloker entered text into the multiline field ([field.textarea](/docs/template-builder/reference/field.textarea)).
- [plugin.toloka](/docs/template-builder/reference/plugin.toloka): To customize the task layout.

<Button themeColor="light" view="default" size="l" type="link" url="https://ya.cc/t/aAynLBRt3ttCCg">View example</Button>

<p></p>

<Button themeColor="light" view="default" size="l" type="link" url="/docs/guide/troubleshooting/support?form-source=template-builder-operations-play-audio">Contact support</Button>