To allow Tolokers to select areas only with rectangles, add the `rectangle` key to the `shapes` property and set its value to `true`.

```json
{
    "shapes": {
      "rectangle": true
    }
}
```

  <Button themeColor="light" view="default" size="l" type="link" url="https://ya.cc/t/ZtJ6f56P3ttFCb">View example</Button>
