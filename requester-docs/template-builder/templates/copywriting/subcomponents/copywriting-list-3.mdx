Use the [field.checkbox](/docs/template-builder/reference/field.checkbox) component.

<Cut title="Show code">

```json
{
  "type": "helper.if",
  "condition": {
    "type": "condition.equals",
    "data": {
      "type": "data.output",
      "path": "verdict"
    },
    "to": "bad"
  },
  "then": {
    "type": "field.textarea",
    "data": {
      "type": "data.output",
      "path": "error_description"
    },
    "label": "Describe the error",
    "validation": {
      "type": "condition.required",
      "hint": "enter something in the box"
    }
  }
}
```

</Cut>

<Button view="defaultInverse" size="l" type="link" url="https://ya.cc/t/-Zd32wl73ttFQ3">View example</Button>