Use the [field.button-radio-group](/docs/template-builder/reference/field.button-radio-group) component.

<Cut title="Show code">

```json
{
  "type": "helper.if",
  "condition": {
    "type": "condition.equals",
    "data": {
      "type": "data.output",
      "path": "verdict"
    },
    "to": "bad"
  },
  "then": {
    "type": "field.textarea",
    "data": {
      "type": "data.output",
      "path": "error_description"
    },
    "label": "Describe the error",
    "validation": {
      "type": "condition.required",
      "hint": "enter something in the box"
    }
  }
}
```

</Cut>

<Button themeColor="light" view="default" size="l" type="link" url="https://ya.cc/t/qXZ539523ttFPL">View example</Button>