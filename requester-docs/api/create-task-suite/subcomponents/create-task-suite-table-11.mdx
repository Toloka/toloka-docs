**integer \| required if**<br/><br/>Required if the parameter is not used when creating a task suite `allow_defaults=true`, and the overlap is not specified in the pool parameters (in the [defaults.​default_​overlap_for_​new_task_suites](/docs/api/create-pool#default_overlap_for_new_task_suites) key).<br/><br/>Task suite overlap.

<Note mode="warning" title="Warning">

Please note that overlap you set when creating task suites has priority over the overlap you set during the [pool creation](/docs/api/create-pool).

</Note>