**integer \| required**<br/><br/>The overlap for task suites that are uploaded to the pool (used if the `allow_defaults=true` parameter is set at upload).

<Note mode="warning" title="Warning">

Please note that overlap you set when [creating task suites](/docs/api/create-task-suite) has priority over the overlap you set during the pool creation.

</Note>