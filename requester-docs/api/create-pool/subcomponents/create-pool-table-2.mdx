**integer**<br/><br/>The overlap for tasks that are uploaded to the pool (used if the `allow_defaults=true` parameter is set at upload).

<Note mode="warning" title="Warning">

Please note that the overlap you set when [uploading tasks](/docs/api/create-task) has priority over the overlap you set during the pool creation.

</Note>