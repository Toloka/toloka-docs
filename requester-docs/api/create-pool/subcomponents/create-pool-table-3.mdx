**string \| required if**<br/><br/>Required if selective review is used.<br/><br/>How to count tasks completed by the Toloker:

- `POOL` — Count completed pool tasks.
- `PROJECT` — Count completed project tasks.