**boolean**<br/><br/>Validation parameters for JSON objects:

- `true` — Issue a bonus if the JSON object with bonus information passed validation. Otherwise, skip the bonus.
- `false` — Stop the operation and don't issue bonuses if at least one JSON object didn't pass validation.<br/><br/>The default value is `false`.