**boolean**<br/><br/>Flag of the response received as a result of [merging identical tasks](/docs/api/tasks#task-merge). Value:

- `true` — The response was recorded when identical tasks were merged.
- `false` — Normal Toloker response.