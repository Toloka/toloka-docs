_object_ \| **required if**<br/><br/>Required if `configs[].collector_config.type` is equal to one of the values:

- `GOLDEN_SET`
- `MAJORITY_VOTE`
- `ASSIGNMENT_SUBMIT_TIME`<br/><br/>Parameters for collecting data (depends on the quality control rule specified in the `type` key).