_string_<br/><br/>Type of client application:

- `BROWSER` — A web browser.
- `TOLOKA_APP` — Toloka mobile app.

Value of `operator`:

- `EQ` — Equal to.
- `NE` — Not equal to.

Value of `value` if data couldn't be extracted — `UNKNOWN`.