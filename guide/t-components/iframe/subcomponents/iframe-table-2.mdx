Height of the frame in pixels. Set in the following units:

- Pixels. For example, `height="100px"`;

- Percentage of the size of the parent element. For example, `height="100%"`.<br/><br/>You can also use a formula for setting the width. For example, `height="calc(100%-30px)"`.